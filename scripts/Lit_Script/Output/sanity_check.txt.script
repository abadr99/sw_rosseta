set -o pipefail;set -x;{ { set +x; } 2>/dev/null && echo 'RUN: at line 1': 'echo "LLVM lit test infrastructure is successfully wired up!"' >&2 && { set -x; } 2>/dev/null && {   echo "LLVM lit test infrastructure is successfully wired up!"; }; }
